[project]
name = "pacmangym"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.0.0",
    "pillow>=11.1.0",
    "pyglet>=2.0.20",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
